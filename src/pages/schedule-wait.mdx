---
layout: ../layouts/main.astro
title: Programme
---
import Utils from '../utils.js';
import current from '../data/current.yaml';

export const ey = Utils.editionYear(current.start_date);
export const endFormat = Intl.DateTimeFormat('fr', {
  timeZone: 'Europe/Paris',
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
export const end = endFormat.format(Utils.resolveDate(current.timeline.cfp.publish, ey));



Le programme sera disponible le {end}.
