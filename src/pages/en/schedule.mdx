---
layout: ../../layouts/main.astro
title: Planning
---
import Utils from '../../utils.js';
import current from '../../data/current.yaml';
import Redirect from "../../layouts/Redirect.astro";

export const endFormat = Intl.DateTimeFormat('en', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
export const end = endFormat.format(Utils.parseDate(current.timeline.cfp.publish));



{current.program_show && current.program_link ? (
  <Redirect target={current.program_link} />
) : (
  <p>The program will be available on {end}.</p>
)}